query getAllEvents {
  getEvents {
    id
    hostId
    membersInfo {
      id
      type
    }
    endTime
    startTime
    information {
      eventName
      description
    }
    place {
      name
      address
      coord {
        latitude
        longitude
      }
    }
  }
}

query getEventByCoord($input: CoordInput!) {
  getEventBaseOnPos(input: $input) {
    id
    hostInfo {
      id
      name
      email
      avatar {
        id
        data
        contentType
      }
    }
    endTime
    startTime
    information {
      eventName
    }
    place {
      coord {
        latitude
        longitude
      }
    }
  }
}

query getEventById($id: String!) {
  getEventById(id: $id) {
    id
    hostId
    membersInfo {
      id
      type
    }
    endTime
    startTime
    information {
      eventName
      description
    }
    place {
      name
      address
      coord {
        latitude
        longitude
      }
    }
  }
}
