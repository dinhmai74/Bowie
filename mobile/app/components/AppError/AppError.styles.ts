import { StyleSheet } from 'react-native'
import { spacing } from 'theme'

export const appErrorStyles = StyleSheet.create({
  wrapper: {
    justifyContent: 'center',
  },
  text: {
    marginVertical: spacing[1],
  },
})
